//const notesCtrl = {}
/*
const Note = require('../models/Guides');

guidesCtrl.getNotes = async (req, res) => {
   const notes = await Guide.find(); //trae todos los datos de la colecci'on del Model Note
                // esta es una funcion async por o que tengo que esperarla
   res.json(notes);
};
//minuto 13 stack MERN video 7 
//notesCtrl.getNotes = (req, res) => res.json({message: []}) // pido el mensaje almacenado en Note,js

module.exports = notesCtrl;

*/